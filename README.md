# network-arch-project
run `run_experiments.py` to run the network simulation and get some data outputs.\
run `generate_plots.py` afterwards to get some more plots.\
`requirements.txt` contains python requirements. run `pip install -r requirements.txt` to install them